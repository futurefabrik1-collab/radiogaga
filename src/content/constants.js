// Shared constants — avoid magic numbers scattered across modules.

export const WORDS_PER_SECOND = 2.5;  // average spoken words per second
export const MP3_BITRATE = '128k';
export const SAMPLE_RATE = 44100;
